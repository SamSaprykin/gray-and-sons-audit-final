import { Layout } from "@/components/audit/Layout";

import PackagesData from "../../../audit-data/performance/packages.mdx";

export default function Packages() {
  return (
    <Layout>
      <PackagesData />
    </Layout>
  );
}