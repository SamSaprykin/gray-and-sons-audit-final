import { Layout } from "@/components/audit/Layout";

import Redirects from "../../../audit-data/seo/redirects.mdx";

export default function Tmep() {

  return (
    <Layout>
      <Redirects />
    </Layout>
  );
}