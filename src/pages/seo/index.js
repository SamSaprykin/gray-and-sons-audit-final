import { Layout } from "@/components/audit/Layout";

import Redirects from "../../../audit-data/seo/general.mdx";

export default function SeoMain() {

  return (
    <Layout>
      <Redirects />
    </Layout>
  );
}